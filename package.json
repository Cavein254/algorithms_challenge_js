{
  "name": "algorithm",
  "version": "1.0.0",
  "description": "1. `lonelyInt` - find a unique integer from an array's list",
  "main": "amstrong.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "npm-watch"
  },
  "watch": {
    "build": ".*.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^3.0.1",
    "npm-watch": "^0.11.0",
    "watch": "^1.0.2"
  }
}
